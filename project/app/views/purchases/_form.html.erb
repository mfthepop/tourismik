<%= form_for(@purchase) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :purchase_id %><br>
    <%= f.number_field :purchase_id %>
  </div>
  <div class="field">
    <%= f.label :ammount %><br>
    <%= f.number_field :ammount %>
  </div>
  <div class="field">
    <%= f.label :date_of_purchase %><br>
    <%= f.date_select :date_of_purchase %>
  </div>
  <div class="field">
    <%= f.label :num_of_reservations %><br>
    <%= f.number_field :num_of_reservations %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
